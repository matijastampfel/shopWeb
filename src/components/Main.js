import React, { Component } from 'react';
export default class Main extends Component{
     render() {

         return (

             <div>

            <button> onClick={this.props.changeUserName}</button>
             </div>

         );


     }

 }
